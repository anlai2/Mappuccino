import React, { Component } from 'react';
import { Text, View } from 'react-native';

class FilterModal extends Component {
  render() {
    return (
      <View>
        <Text> TEST FILTER SCREEN </Text>
      </View>
    );
  }
}

export default FilterModal;
